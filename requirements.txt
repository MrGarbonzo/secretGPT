# secretGPT Hub Requirements
# Phase 1: Core dependencies

# Secret AI SDK and dependencies
# Using SDK 0.1.5 (latest) with testnet configuration via env vars
secret-ai-sdk==0.1.5
secret-sdk>=1.8.1
langchain==0.3.13
langchain-core==0.3.28
langchain-ollama==0.2.2

# OpenAI client for Secret AI OpenAI-compatible endpoint
openai>=1.0.0

# Core dependencies
pydantic==2.10.4
pydantic-settings==2.6.1
python-dotenv==1.0.0
httpx==0.27.2
aiohttp==3.11.11

# Logging and utilities
structlog==24.1.0

# Phase 2: Web UI
fastapi>=0.110.0
uvicorn==0.24.0
jinja2==3.1.2
python-multipart==0.0.6
cryptography==44.0.0

# Phase 4: MCP Integration (to be added)
# Additional dependencies for MCP client